@model dynamic

@{
    ViewData["Title"] = "Получить статистику";
}

<h2>Получить статистику</h2>

<hr />
<h4>Статистика игрока</h4>
<div class="row">
    <div class="col-md-4">
        <form asp-action="PlayerStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="playerId" class="control-label"></label>
                <select name="playerId" class="form-control" asp-items="@ViewBag.players"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Получить статистику игрока" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<hr />
<h4>Статистика команды</h4>
<div class="row">
    <div class="col-md-4">
        <form asp-action="TeamStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="teamId" class="control-label"></label>
                <select name="teamId" class="form-control" asp-items="@ViewBag.teams"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Получить статистику команды" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<hr />
<h4>Статистика вида спорта</h4>
<div class="row">
    <div class="col-md-4">
        <form asp-action="SportStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="sportId" class="control-label"></label>
                <select name="sportId" class="form-control" asp-items="@ViewBag.sports"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Получить статистику вида спорта" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<hr />
<h4>Лучшие игроки по типу события</h4>
<h6>Поддерживаются только события для одного игрока.</h6>
<div class="row">
    <div class="col-md-4">
        <form asp-action="SingleEventTypeStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="eventTypeId" class="control-label"></label>
                <select name="eventTypeId" class="form-control" asp-items="@ViewBag.eventTypes"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Получить статистику вида спорта" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<hr />
<h4>Самые часто играемые виды спорта</h4>
<div class="row">
    <div class="col-md-4">
        <form asp-action="PopularityStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Получить статистику" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<hr />
<h4>Самые интересные виды спорта</h4>
<h6>Среднее число событий в течение одной игры.</h6>
<h6>Игры, в течение которых ничего не произошло, не учитываются.</h6>
<div class="row">
    <div class="col-md-4">
        <form asp-action="ActivityStatistics">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Получить статистику" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
