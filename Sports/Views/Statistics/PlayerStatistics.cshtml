@model IEnumerable<Sports.ViewModels.Statistics>

@{
    ViewData["Title"] = "Статистика игрока";
}

<h2>Статистика игрока @ViewData["Name"]</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        @foreach (var eventType in Model)
        {
            <dt>
                @eventType.Name
            </dt>
            <dd>
                @eventType.Count
            </dd>
        }
        @if (Model.Count() == 0)
        {
            <dt>
                Бесполезный игрок.
            </dt>
            <dd>
                За всю свою карьеру он ни разу ничего не сделал.
            </dd>
        }
    </dl>
</div>
<div>
    <a asp-action="Index">Вернуться</a>
</div>
